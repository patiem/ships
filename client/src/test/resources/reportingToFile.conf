#
# author: Sandor
# since: 2018-01-23
#

#
# Use this segment to enable a particular destination
# of game messages.
#

destination {
  # Recognizible options : file / socket / logger / window / off (default)
  target = "file"
}

options {
  file {
    # An arbitrary file name, prefarably with .log extension.
    name="../target/test-reporting.log"
  }
}