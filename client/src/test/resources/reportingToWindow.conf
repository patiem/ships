#
# author: Sandor
# since: 2018-01-23
#

#
# Use this segment to enable a particular destination
# of game messages.
#

destination {
  # Recognizible options : file / socket / logger / window / off (default)
    target = "window"
}

options {
  window {
    title="Snarky Reporter"
    height=600
    width=300
  }
}